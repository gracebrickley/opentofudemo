# Terramate stack metadata for KIND cluster

stack {
  name        = "KIND Cluster"
  description = "Host Kubernetes cluster using KIND (Kubernetes in Docker)"
  tags        = ["platform", "kind", "kubernetes", "layer-1"]
  id          = "platform/kind-cluster"
}

